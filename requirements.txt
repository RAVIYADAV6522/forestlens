# Core app — enough to run the UI and the tested measurement logic
streamlit>=1.40
numpy>=1.26,<3
pillow>=10.0

# Raster / geospatial: needed to read GeoTIFF metadata (and so the GSD) and to export GeoJSON
rasterio>=1.3

# CPU-only torch wheels. On Linux the default PyPI wheel is a ~2.5 GB CUDA build, which is
# useless on a CPU host and slow to install. Local version tags (2.x.y+cpu) sort above the
# plain version under PEP 440, so this index wins when it has a matching wheel.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2
torchvision>=0.17

# Pretrained individual-tree detection
deepforest>=1.3

# Crown-mask refinement. Optional: without it the app reports a labelled bounding-box proxy.
# Distribution name is "sam-2" (not "sam2"); the normalised names differ and pip
# rejects the mismatch, so it must be spelled this way.
sam-2 @ git+https://github.com/facebookresearch/sam2.git ; python_version < "3.13"

# Deployment (scripts/deploy_hf_space.py); also arrives transitively via deepforest
huggingface-hub>=0.25

# Tests
pytest>=8.0
